Creates new device type
Requires DeviceType object and creates device type
---
tags:
  - DeviceType
parameters:
  - in: body
    name: body
    required: true
    schema:
        type: object
        $ref: '#/definitions/DeviceType'
responses:
  201:
    description: Success
    schema:
      type: object
      required:
        - content
      properties:
        content:
          $ref: '#/definitions/DeviceType'
