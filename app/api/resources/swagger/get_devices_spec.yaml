Gets all associated devices
---
tags:
  - Device
responses:
  200:
    description: Success
    schema:
      type: object
      required:
        - devices
      properties:
        devices:
          type: array
          items:
            $ref: '#/definitions/Device'

